#include <stdio.h>
int main(){
    int a[2][2];
    int b[2][2];
    int sum[2][2];
    printf("enter the first matrix");
    for(i=0; i<2; i++)
    {
        for(int j=0;j<2,i++)
        {
            printf("enter the element a[%d][%d]",i,j);
            scanf("%d",&a[i][j]);
        }
    }
        return o;
}
